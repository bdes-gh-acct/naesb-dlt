import { PageContainer } from '@common/page';
import { ChannelGrid } from './Grid';

export const ChannelList = () => {
  return (
    <PageContainer title="Channels">
      <ChannelGrid />
    </PageContainer>
  );
};
